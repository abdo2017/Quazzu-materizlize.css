jQuery(document).ready(function($) {
// init menu nav  for mobile
    $(".button-collapse").sideNav();
// init carousel testimonials
  $('.carousel.carousel-slider').carousel({fullWidth: true});
// init modal
    $('.modal').modal();
// init select list 
    $('select').material_select();
	

});
